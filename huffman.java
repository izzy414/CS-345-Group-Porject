// Main function that takes the user input
// Frequencies function gets frequencies of string and puts it into hashmap (takes arguement of the string)
// Tree function creates the tree (takes the hashmap as an argument)
   // Node object reates the nodes of the tree with number frequency, letter, L = 2K+1 and R = 2k+2
   // Max heap array
   // 
// Print the data in binary tree form
// Print the data in tree form
